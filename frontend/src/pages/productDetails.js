import React from 'react'

const ProductDetails = () => {
    return (
        <div>productDetailP
            <div>
                call the common api file (which store api fom backend)
            </div>
        </div>
    )
}

export default ProductDetails